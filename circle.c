#include<stdio.h>
#include<conio.h>
main()
{
    float r,area;
    printf("enter the radius of circle:");
    scanf("%f",&r);
    area=(22.0/7)*r*r;
    printf("the area of circle is :%f",area);
    getch();
}
